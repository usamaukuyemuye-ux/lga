# Firebase credentials are configured via firebase-applet-config.json

# Africa's Talking SMS Gateway (Optional: also configurable via School Settings in the app)
AFRICASTALKING_USERNAME=sandbox
AFRICASTALKING_API_KEY=
AFRICASTALKING_SENDER_ID=LITTLEGEMS
